# T4.6 — The adversarial suite as DURABLE CI

- **Date:** 2026-07-07
- **Task:** Phase 4, T4.6 (plan §7 PHASE 4 + §6.3). Consolidate the Phase-4 attack
  matrix into a tagged `test/adversarial/` suite in the demo (`mix test --only
  adversarial` + a numbered `demo/ci.sh` step), covering the six required categories,
  each with a POSITIVE CONTROL so denials are non-vacuous. Red paths ARE the suite;
  one anti-tautology probe per category (6 probes) confirms each guard is load-bearing.
- **Vision doc:** "Running the business" (:880–:906) in full — the time-boxed-grant
  (:892), immutable-and-shreddable (:894), two-planes/two-operator-paths (:898);
  "The honest edges" break-glass (:951) + token-blind/inference (:947) bullets. Spec
  language matched exactly (documented residues named, never over-claimed).
- **Built ON (never duplicated):** T1.6 reveal-grant (`Samen.Reveal.Grants`, DB CHECK
  `rvg_distinct_party`), T1.7 crypto-shred (`Samen.Erasure`), T2.2 `aud_event`, T2.9
  oracle (`Samen.NoPlaintextPii`), T4.1 impersonation + `Samen.Api.PiiResolution`,
  T4.2 token-blind aggregate + `Samen.Aggregate.read_all` chokepoint, T4.3 audit chain
  + WORM anchor (`Samen.AuditChain`, `Samen.Anchor.LocalWorm`), T4.4 break-glass
  (`Samen.BreakGlass`), T4.5 floors (`Samen.Aggregate.Privacy`). The suite exercises
  the REAL demo resources (`Demo.Crm.Contact`, `Demo.Aggregate.*`, `Demo.BillingScope`,
  `Demo.SupportScope`, `Demo.Identity.Org`) — not fixtures.

## Verdict: GREEN

Both suites + root `bash ci.sh` green BEFORE and AFTER, `--warnings-as-errors` clean.
Every category ships red paths (the suite IS the red paths) + a positive control; each
category carries an anti-tautology probe (source sabotage in a project-local scratch
dir, confirm the matching case FLIPS, revert with identical restore, scratch removed).

### Environment results (run by me, not asserted from memory)

| Check | Before | After |
|---|---|---|
| `samen_core` `mix test --warnings-as-errors` | 752 | **752** (unchanged — suite lives in demo) |
| `samen_core` `mix compile --warnings-as-errors --force` | exit 0 | **exit 0**, warning-clean |
| `demo` `mix test --warnings-as-errors` (default) | 399 | **399** (48 adversarial EXCLUDED by default) |
| `demo` `mix test --only adversarial --warnings-as-errors` | — | **48 passed** (399 excluded) |
| demo CI gate steps | 16 | **17** (+`mix test --only adversarial`) |
| root `bash ci.sh` (6 spikes + core + demo + 17-step demo gate) | PASS | **PASS — ROOT CI: ALL PASSED** |

## What shipped (files)

All under `demo/` — the suite is the demo dogfood's own attack matrix.

- `demo/test/adversarial/impersonation_bypass_matrix_test.exs` (10) — **category 1**.
  Under an impersonation session with NO grant, every egress on the real vaulted
  `Demo.Crm.Contact` shows `••••`/absent: LiveView (`Phoenix.HTML.Safe`), JSON
  (`%Masked{}` `Jason.Encoder`), webhook payload (`Samen.Webhook.Payload.build/3`),
  CSV/iodata (interpolation), logs (`inspect`/`to_string`), error messages (an
  exception interpolating the field), plus the T3.11 resolver re-run. POSITIVE CONTROL:
  a live reveal grant on top surfaces plaintext on the SAME paths. Also attacks the
  REAL operator API-KEY absent posture (`%Ash.ForbiddenField{}` → omitted from payload).
- `demo/test/adversarial/reveal_grant_abuse_test.exs` (13) — **category 2**. Self-approval
  blocked at BOTH the policy layer (`{:error, :self_approval}`, no grant row) and the DB
  layer (raw SQL insert with `granted_by == requestor_id` raises `rvg_distinct_party`);
  expired grant deny-on-read; renew-in-place (`attempt_extend` fails, `expires_at`
  unchanged); grant-row/audit-chain tamper (edit → `:hash_mismatch`; append-only trigger
  raises on UPDATE/DELETE); **requestor-approver collusion via a second account** —
  ALLOWED by the mechanics (documented residue) but its two colluding ids are ASSERTED
  VISIBLE (grant row `requestor_id`+`granted_by`; both events in `audit_for/1` with both
  actor ids). POSITIVE CONTROL: the legitimate path succeeds; the lone-operator self-serve
  path is dead.
- `demo/test/adversarial/aggregate_differencing_test.exs` (6) — **category 3**. Bypass
  attempts against the `Samen.Aggregate.read_all` chokepoint: FILTER bypass (a `:query`
  narrowing to a count-of-one cohort still suppresses); raw-read bypass (a non-aggregate
  actor is default-denied; the aggregate actor's DIRECT `Ash.read` is unfloored — proving
  the chokepoint is the ONLY floor-applying path, and the dashboard reads only through it);
  repeated/differencing queries (isolating count-of-one suppressed, ledger accounts BOTH
  per-cohort, above-floor residue documented honestly per T6.6); l-diversity homogeneity
  bypass (a big-but-uniform cohort suppresses). POSITIVE CONTROL throughout (a >=k, l-diverse
  cohort releases in the same read).
- `demo/test/adversarial/audit_tamper_test.exs` (8) — **category 4**. Edit (`:hash_mismatch`),
  delete (`:seq_gap`), append-only trigger (ordinary UPDATE/DELETE raise), REWRITE-vs-anchor
  (a wholesale rebuild that `verify_chain`s clean is caught by the WORM anchor →
  `:anchor_divergence`), truncate-below-anchor (`:truncated_below_anchor`), tenant view
  surfaces `chain_verified: false` + no cross-org leak. POSITIVE CONTROL: untampered chain
  verifies AND `verify_against_anchor` → `:verified`.
- `demo/test/adversarial/break_glass_abuse_test.exs` (9) — **category 5**. Budget breach →
  DENY + auto-suspend → every path denies (break-glass, routine, impersonation-open);
  breadth-not-depth; KMS-down bypass attempt FAILS CLOSED (`:unavailable`) even with a
  writable local audit, deny-RECOVERABLE on heal; local-entry tamper detected at
  reconciliation (`{:local_tamper, _}`, NOTHING anchored); no-reason/non-role refuse.
  POSITIVE CONTROL: a legitimate break-glass succeeds with a durable local entry.
- `demo/test/adversarial/crypto_shred_control_plane_test.exs` (3) — **category 6**. Crypto-shred
  completeness against the CONTROL-PLANE tiers: an `aud_chain` reveal event carrying the
  subject's key-destroyable ciphertext SURVIVES post-shred (chain still `verify_chain`s,
  event row present) while the subject ciphertext is PERMANENTLY undecryptable
  (`{:error, :shredded}`); the impersonation session row is token-only (no `pii_`/ciphertext
  column) and survives unchanged; **the destruction oracle run is EXTENDED in CI** — the
  post-shred `Samen.NoPlaintextPii.run(mode: :post_shred)` PASSES on a control-plane-seeded
  subject (aud_chain CI tier + DB-content + KMS-attestation), the in-process equivalent of
  the demo gate step. POSITIVE CONTROL: BEFORE shred the SAME ciphertext decrypts.

### Wiring

- `demo/test/test_helper.exs` — `ExUnit.configure(exclude: [:adversarial])`. The suite is
  EXCLUDED from the default `mix test` (keeps the dogfood fast at 399); `--only adversarial`
  re-includes it. Tag on every file: `@moduletag :adversarial`.
- `demo/ci.sh` — **step 17/17** `mix test --only adversarial --warnings-as-errors` (a numbered
  gate step after the 16 verifiers), with a comment block enumerating the six categories.

## Anti-tautology probes (HARD RULE 2) — one guard sabotaged per category

Each probe backed up the target file to a project-local scratch dir
(`.t46_antitaut_scratch/`, since removed), sabotaged ONE guard, ran the matching category,
confirmed the RED path FLIPPED, reverted from the backup (`diff` identical, 0 `SABOTAGED`
markers), and re-verified green.

| # | Category | Guard sabotaged | Flip observed |
|---|---|---|---|
| 1 | impersonation | `Samen.Api.PiiResolution` impersonation branch → reveal plaintext with no grant | 8/10 flip: plaintext `bob@secret.test` leaked through every egress |
| 2 | reveal-grant | `Samen.Reveal.Grants.approve` policy self-approval guard → always false | 2/12 flip: `{:error, :self_approval}` failed (the DB CHECK still caught it — belt-and-suspenders) |
| 3 | aggregate | `Samen.Aggregate.Privacy.suppress_row` → never suppress | 4/6 flip: raw count-of-one values (999000/424242) leaked instead of `%Suppressed{}` |
| 4 | audit tamper | `Samen.AuditChain.do_verify` hash-mismatch check → skipped | 2/8 flip: edit undetected + tenant view `chain_verified` stayed true |
| 5 | break-glass | `Samen.BreakGlass.live_kms_reveal` → manufacture plaintext on `:unavailable` | 1/9 flip: KMS-down deny turned into a bypass |
| 6 | crypto-shred CP | `Samen.Kms.FileBacked.shred` → no-op (DEK survives) | 2/3 flip: post-shred `unwrap` returned the surviving key; oracle post-shred check failed |

All six guards are load-bearing, non-vacuous discriminators. All reverts confirmed
byte-identical; `grep -rn SABOTAGED` over `samen_core/lib demo/lib demo/test` == 0 at finish.

## Honest edges preserved (no silent new residues)

- **Requestor-approver collusion via a second account** is ALLOWED by the mechanics (the
  doc's dual-control honest edge — two coordinating identities cannot be stopped by
  distinct-party alone). The suite does NOT claim it is blocked; it ASSERTS the residue is
  VISIBLE in the tenant-readable audit (both ids on the grant row + both events in the
  trail). This matches §6.3 exactly ("documented residue + audit-visible").
- **Above-floor differencing** is NOT defended today (cross-query budget / DP / t-closeness
  = posture under construction, plan T6.6). The suite documents the honest state: the
  isolating count-of-one side IS suppressed (k-anon enforced), the ledger records BOTH reads
  per-cohort, but NO read is blocked by the budget (`over_threshold? == false`). No over-claim.
- **The break-glass local-write→anchor window** and **KMS-availability-for-decrypt** residues
  from T4.4 are left named, not papered over — the KMS-down deny is deny-recoverable, the
  local audit is the accountability traded FOR synchronous anchoring, never for availability.

## Seams / notes

- The suite lives in `demo/` (not `samen_core/`) because it must exercise the real
  composed demo resources + the demo's wired config (reveal-grant repo, impersonation repo,
  aggregate projections, aud_chain repo fallback, break-glass tables). samen_core's own
  kernel-level red paths (reveal_grants_test, audit_chain_test, break_glass_test, etc.)
  remain — this is the CONSOLIDATED end-to-end matrix on top, not a replacement.
- No AWS/S3 was faked: the WORM anchor uses the faithful `Samen.Anchor.LocalWorm` adapter
  (append-only file, fsync, verify-on-read); the S3ObjectLock production skeleton is
  untouched (T4.3). The KMS uses `Samen.Kms.FileBacked` (the external-store simulator that
  can `simulate_outage/1`) — the load-bearing KMS-down and shred red paths are real.
