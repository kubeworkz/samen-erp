# ADR Index

Every accepted architecture decision in the foundry, one line each: number → title → the
decision in one clause. Read the ADR itself before changing anything it governs (per
`CLAUDE.md`) — this table is a map, not a substitute.

Numbering is chronological, not thematic; the columns below group them by concern so you can
scan for "everything about the vault" or "everything about the operator plane" without
reading all 49.

## Vault, crypto-shred, audit

| # | Title | Decision |
|---|---|---|
| [001](ADR-001-key-hierarchy.md) | The per-subject external-KMS key hierarchy | Each subject's vault key lives in an external KMS outside the Postgres/WAL surface, so destroying it (crypto-shred) makes every tier unrecoverable at once and a PITR restore cannot resurrect it. |
| [002](ADR-002-worm-anchor.md) | Hash-chained audit + WORM anchor | The audit log is a hash-chained, append-only, tenant-readable table (trigger-refused edits) periodically anchored to a WORM store, carrying tokens only so it stays crypto-shreddable. |
| [003](ADR-003-encryption-lib.md) | Encryption-library integration for the per-subject vault | Reject AshCloak / a `Cloak.Vault`-per-subject wrapper; build a thin custom vault layer directly on OTP `:crypto` AES-256-GCM driven by the `Samen.Kms` behaviour. |

## Scope packaging & the framework extraction

| # | Title | Decision |
|---|---|---|
| [004](ADR-004-scope-packaging.md) | Scope packaging: how a universal scope ships and how a host mounts it | Every universal scope ships as a library-authored Ash blueprint macro (`use Samen.Scopes.X`) that a host materializes into its own namespace/repo — the packaging seam every later scope copies. |
| [005](ADR-005-operator-plane-migration-extraction.md) | Extract the `aud_chain` operator-plane migration into a shared core helper | Extract the hash-chain audit migration into a shared core template so every host mounting the operator plane gets it without hand copy-paste. |
| [006](ADR-006-abbrev-registry-scoping.md) | Abbrev registry: global-vs-per-host scoping | Defer the registry refactor for now; adopt per-host-namespaced ownership inside one global registry file (Option B) as the target, explicitly behind a future generator. |
| [007](ADR-007-rollup-cron-worker.md) | Rollup refresh as a real AshOban cron worker vs a plain function | Define the spec-registration bridge for rollups as real cron workers; defer wiring vertical rollups until a 3rd vertical confirms the generalized `Spec` shape. |
| [008](ADR-008-ui-kit.md) | Samen product-UI kit (function components + shared CSS) | Unify the three approved product-UI mockups into one shared function-component kit + `:root` token CSS (later superseded by ADR-009). |
| [009](ADR-009-samen-web.md) | `samen_web`: the framework UI library + the two-plane mountable-module pattern | Promote the inherited product UI (component kit + CRM/Billing/Support LiveViews) out of driftwood into a shared `samen_web` lib, parameterized by a `Samen.Web.Mount` struct and driven by a `Samen.Web.Plane` two-plane (tenant/operator) abstraction. |

## The operator / control plane

| # | Title | Decision |
|---|---|---|
| [010](ADR-010-operator-plane.md) | The Operator / SaaS-company plane: accounts ARE tenant orgs, platform billing over tenants, the SaaS's own help desk, and the identity line | The SaaS is itself an org (the operator org) mounting the same universal scopes over its tenant orgs as accounts; the identity line (tenant-admin PII clear, tenant end-customer PII masked) is drawn purely by composing the existing `OrgScope` + `PiiResolution` primitives — no new masking code. |
| [011](ADR-011-crm-enrichment.md) | CRM enrichment: contact/company detail, activity timeline, email/sequences, prospecting, social — a real CRM in `samen_web` | Build a world-class CRM (detail pages, activity timeline, email/sequences, prospecting, social) entirely in `samen_web` so every vertical inherits it. |
| [012](ADR-012-crossplane-chat.md) | Cross-plane realtime chat, catalog-driven object unfurl, and the participant identity model | Ship a flagship framework-level chat spanning the operator↔tenant plane boundary, with a catalog-driven, per-viewer masking-aware object unfurl and a 3-state participant identity model. |
| [013](ADR-013-demo-coherence.md) | Demo coherence: home, current-org resolution, workspace switcher, and the 5-tenant seed | Fix Driftwood's entry model — session-resolved current org, a real workspace switcher, the operator "Open account →" drill-in, a 5-brokerage seed — so the three-plane model is navigable without typed UUIDs. |

## WS-A "Product Reality"

| # | Title | Decision |
|---|---|---|
| [014](ADR-014-delivery-adapter-fail-honest.md) | Outbound delivery adapter contract, fail-honest send semantics, and abbrev-derived suppression | Replace the always-succeeds stub email adapter with a pluggable, fail-honest `Samen.Delivery.Adapter` behaviour, and derive the suppression check from the abbrev instead of a hardcoded table name. |
| [015](ADR-015-default-deny-cdc-classifier.md) | Default-deny CDC/aggregate classifier for freeform content columns | Flip the CDC/aggregate projection default for freeform string/text/map columns from opt-out-of-flagging to opt-in-allowlisted (vault-routed or two-reviewer `non_pii!`), closing the silent-PII-mirroring gap. |
| [016](ADR-016-kit-list-crud-notifications.md) | Kit list/CRUD primitives, keyset pagination contract, and the masking-aware notifications engine + inbox | Ship kit-level `simple_form`/`modal`/`list_view` primitives + a `ListLive` behaviour, a keyset-pagination reads contract, and a masking-aware notifications delivery engine + inbox — framework-first. |

## WS-B "Operator Cockpit v1"

| # | Title | Decision |
|---|---|---|
| [017](ADR-017-subscription-movement-ledger.md) | Subscription-change ledger via the StatusChange seam (not audit-event reconstruction) | Capture MRR-movement events with a dedicated append-only `Billing.SubscriptionEvent` resource, written by a change modeled on the proven `StatusChange` seam — not reconstructed from the audit tier. |
| [018](ADR-018-domain-sourced-rollup-spec.md) | Implement the `:source :domain` rollup Spec dimension now (resolving the ADR-007 defer) | Implement the previously-deferred `:source :domain` rollup dimension so a domain-table-sourced rollup (the revenue-movement rollup) can register, and extend the destruction oracle to cover it. |
| [019](ADR-019-health-score-model.md) | Composite, explainable per-tenant health score (compute layer, not a new PII surface) | Compute a composite, explainable per-tenant health score (`HealthBreakdown` with weighted factors) purely over already-assembled operator-read data — no new PII surface. |
| [020](ADR-020-feature-flag-evaluation-engine.md) | Feature-flag evaluation engine: kernel placement, deterministic bucketing, non-PII keys, fail-safe kill switch | Build the flag evaluation engine (`evaluate/2`) in the kernel with deterministic `phash2` bucketing, non-PII targeting keys, and a fail-safe kill switch; the admin UI lives in `samen_web`. |
| [021](ADR-021-product-analytics-event-primitive.md) | Product-analytics event capture: a new governed kernel resource (not WideEvent), refusing PII at capture | Capture product events via a new governed kernel resource (`Analytics.ProductEvent`, token-blind by construction) rather than overloading `WideEvent`/`Notifications`/`AuditEvent`, refusing PII-bearing payloads at the capture boundary. |

## WS-D "Builder Joy"

| # | Title | Decision |
|---|---|---|
| [022](ADR-022-generator-emits-running-product.md) | The generator emits a running product (flagged web/API/seed/observability), not a headless data layer | `mix samen.gen.app` scaffolds a real running product (web UI, JSON:API, seeds, observability) behind flags, not just a headless data-and-gate scaffold. |
| [023](ADR-023-abbrev-reserve-allocator-host-namespace.md) | `mix samen.abbrev.reserve` allocator + host-namespaced registry schema (implementing ADR-006 Option B, bounded) | Ship `mix samen.abbrev.reserve` (deterministic propose/reserve) plus a host-namespaced registry schema, implementing ADR-006's deferred Option B, bounded behind the generator. |
| [024](ADR-024-generated-deploy-fail-honest.md) | Generated deploy artifacts are fail-honest, not aspirational | Generated deploy artifacts (Fly/Neon/secrets) must be fail-honest — raising and naming a missing secret rather than shipping an aspirational "just run `fly deploy`" claim. |
| [025](ADR-025-abbrev-verifier-host-partition-followon.md) | Abbrev registry + verifier host-partition (phased follow-on to ADR-023) | **Proposed/deferred** — a phased follow-on to fully host-partition the abbrev registry and its verifier, filed as a bounded decompose item, not yet built. |

## WS-E "Table Stakes UX"

| # | Title | Decision |
|---|---|---|
| [026](ADR-026-files-storage-adapter-fail-honest.md) | Files engine: fail-honest storage adapter behaviour, quarantine-by-default, LiveView upload chokepoint | Build a `Samen.Files.Storage` adapter behaviour (`Local` + an `S3` skeleton, fail-honest) with a LiveView upload chokepoint, quarantine-by-default freshly-uploaded files, and a masking-aware preview surface. |
| [027](ADR-027-search-engine-non-pii-tsquery.md) | Search engine: kernel `Search.query/2` over the non-PII tsvector, registry-gated, org-scoped, ⌘K in samen_web | Build a kernel `Samen.Search.query/2` over the existing non-PII tsvector/registry, org-scoped and ranked, with a `samen_web` ⌘K palette on top. |
| [028](ADR-028-csv-import-export-mask-by-omission.md) | CSV import/export: catalog-driven mapper, export as a first-class masking surface, import through the write chokepoint | Build a catalog-driven CSV mapper where export renders every cell through the same per-plane masking seam as the UI (non-negotiable red path) and import writes through the vault `WriteGuard` chokepoint. |
| [029](ADR-029-self-serve-settings-scope-gated-self-edit.md) | Self-serve settings: profile self-edit through the vault chokepoint, API-key mint UI, host-owned auth boundary respected | Build a `/settings` surface (profile self-edit, API-key management, session list) that writes the user's own vaulted PII through the same `WriteGuard`/`Vault.Change` chokepoint, keeping auth host-owned. |
| [030](ADR-030-responsive-kit-level-pass.md) | Responsive: a kit-level CSS pass (one stylesheet, one shell component) so every vertical becomes mobile-usable at once | Make the whole fleet mobile-usable with one CSS-and-kit-only pass (two breakpoints; sidebar becomes a toggle drawer; tables restack or scroll) — no per-page LiveView changes. |

## Launch (F1–F4)

| # | Title | Decision |
|---|---|---|
| [031](ADR-031-byo-auth-launch-onramp.md) | BYO-auth launch on-ramp: the prod tenant actor is derived from an authenticated session, not a query param | Wire a real session-auth flow into `Samen.Web.CurrentOrg` for one vertical (driftwood), replacing the query-param-derived actor on its prod path. Amended 2026-08-12 by ADR-045 §2 — the `:auth_required?` default is now ARMED in `:prod` (fail-secure); dev/test unchanged. |
| [032](ADR-032-data-residency-us-only.md) | Data residency: US-only, documented (no per-tenant region selection) | Document (no code change) that the single-Postgres-per-host model is US-only with no per-tenant region selection, and name what a real multi-region story would require. |
| [033](ADR-033-in-monorepo-distribution-constraint.md) | Framework distribution stays path-dep-in-monorepo; Hex publishing deferred to a stated trigger | Keep framework distribution as `path:` deps inside the monorepo; defer Hex publishing until a stated trigger, since every consumer today resolves the framework via a path dep. |

## Hardening (F7)

| # | Title | Decision |
|---|---|---|
| [034](ADR-034-nonpii-type-selfclassify-reviewer-gate.md) | The type-level `:non_pii` self-classification is reviewer-gated (two distinct parties) | Honor a host type's `:non_pii` self-classification only behind a valid two-distinct-party clearance (`Samen.NonPii.TypeClearance`, a pure config allowlist); an ungoverned/self-reviewed one falls through to the mask-unknown-by-default PII result — closing the single-party escape hatch, symmetric with the per-column `non_pii!` gate. |

## SaaS-readiness ecosystem + rich types (WS-H) + identity spine (WS-A)

| # | Title | Decision |
|---|---|---|
| [ADR-035](ADR-035-identity-spine.md) | Identity spine architecture: the WS-A A1–A10 contracts | Consumes ADR-037 §5.1 (AshAuthentication REJECT) → hand-built spine on the existing seams: org-less `Identity.Credential` principal + Session/AuthToken/UserIdentity resources, HMAC blind-index email lookup (no plaintext email at rest; `k_bidx` = reserved Kms subject `sys:bidx`, shred-refused), hashed single-use tokens, DB-backed revocable sessions (tenant-plane only), PBKDF2-SHA256 behind `Samen.Auth.Hasher`, assent/nimble_totp/ash_rate_limiter placed in `samen_web` only — `samen_core/mix.exs` untouched. |
| [ADR-037](ADR-037-ash-ecosystem-adoption.md) | Ash-ecosystem adoption evaluation (operator directive M6) | One ADOPT/REJECT verdict per Ash package against INV-1/INV-3/maturity/cost: ADOPT ash_money, ash_archival, ash_paper_trail, reactor, ash_state_machine, ash_oban, ash_rate_limiter (narrow), usage_rules (dev); REJECT ash_authentication, ash_events, ash_ai, ash_geo, ash_csv, ash_double_entry, ash_admin — no verdict weakens a masking claim or removes a verifier tier. |
| [ADR-036](ADR-036-rich-types.md) | Rich property types: the H1–H7 `Ash.Type` contracts, AshMoney-backed `Money`, destructive cents→composite migration | `Samen.Type.Money` wraps `AshMoney.Types.Money` (samen name in catalog, package semantics); non-PII types (Money/Percent/Score/Duration/Priority/URL) self-classify `:non_pii` behind a two-party `TypeClearance`; email/phone stay PII-by-default (personal→vault, org-contact→per-column clearance); Address is a PII composite (`pii_address`); H1 migrates Opportunity + Price via one pre-1.0 destructive data-copy migration per resource (implements ADR-037 §5.2). |
| [ADR-038](ADR-038-adapter-architecture.md) | Adapter architecture: billing + delivery providers, webhook ingress, test posture (WS-B/WS-C) | Core-defined fail-honest `Samen.Billing.Provider` + `Samen.Delivery.Provider` behaviours (adapters translate/transport, core owns state + fetch-on-event convergence); four vendor packages (samen_stripe, samen_postmark, samen_ses, samen_resend) keep core/web vendor-free (INV-4 gate probe with all adapter dirs absent); one shared webhook ingress (signature, replay store, redacted envelopes, Oban DLQ + token-blind operator view) and one `Samen.Web.RateLimit` seam (ash_rate_limiter 1.0.0 manual-plug, non-PII keys, auth + ingress limits, bounded bidx login_failed counter); keyless CI is the gate (fixtures + signed-webhook simulator; live smoke behind `STRIPE_TEST_KEY`/`SAMEN_ESP_LIVE=1`; `SAMEN_POSTMARK_SMOKE=1` no-credential lane skips honestly offline). |
| [ADR-038-A](ADR-038-A-billing-persistence-architecture.md) | Billing persistence architecture (addendum to ADR-038; T106 decides, T108 implements) | The mirror subsystem's convergence watermark + `last_event_id` idempotency marker persist in a **catalog-exempt core infra-table** (`bms_mirror_state`, plain `Ecto.Schema` on the `whk_event` precedent, raw-Ecto writes, nilable `org_id`) — NOT as new columns on the governed `Subscription` blueprint, whose closed-world catalog would force a four-host migration fan-out for kernel bookkeeping. |
| [ADR-039](ADR-039-automation-engine.md) | Automation engine: the WS-E E1/E2/E4/E5/E8 contracts | Ninth scope blueprint (`Workflow`/`Run`/`Reminder`/`Escalation`) on ADR-037's adopted trio — runs compile to Reactor via `Reactor.Builder`, all periodic scans are explicit-cron AshOban triggers, Run/Escalation lifecycles are AshStateMachine; resource-event capture is an in-transaction Oban insert of a values-free non-PII envelope; conditions/interpolations/webhook fields pass ONE write-time eligibility oracle (the CDC projection's non-token classification — c13); runs execute as the workflow OWNER on the tenant plane (no system-actor bypass; vault reads come back `%Masked{}`); loop/rate guards (cycle refusal, depth ≤3, auto-trip breaker) + double-checked two-switch kill (tenant pause, operator kill); SLA-breach + dunning become escalation-primitive clients via adoption seams, domain logic untouched. |
| [ADR-040](ADR-040-lifecycle-substrate.md) | Lifecycle substrate: the WS-E E3/E6/E7 contracts | E3: `Approval` primitives-blueprint resource (AshStateMachine `pending→approved\|rejected\|expired\|cancelled`, `<abbrev>_distinct_party` DB CHECK mirroring `rvg_distinct_party` + policy layer — c12) with a no-persisted-inputs rule (intent lives in governed domain state; handlers re-derive — the ash_events rejection applied to approvals), handler-registry + Ash action-gate faces, same-tx decide+handler, NULL-org documented exception for operator-plane (reveal) kinds; reveal grants become its first client with every existing test unmodified. E6: `archivable true` on `Samen.Resource` attaches ash_archival (preparation variant + explicit restore; `archived_at`, c6), partial-unique-index + declared-composition-cascade + `restore_conflict` conventions, retention purges via `timestamp_field: :archived_at`, hard-delete + crypto-shred unchanged as the terminal path; a 10-scope adoption roster with three exclusion classes (ledgers/mirrors/auth) decomposes T37 into eight mechanical items. E7: `versioned true` attaches AshPaperTrail (`:changes_only` default, `:snapshot` override; `store_action_inputs?` false forever; token-only diffs red-tested), version resources fully governed via `version_extensions`; CMS ContentVersion retired as its client (pre-1.0 break, M6 precedent); the four audit tiers (hash-chain/CDC/versions/run-log) stay disjoint. |

## WS-F work objects (F1)

| # | Title | Decision |
|---|---|---|
| [ADR-041](ADR-041-canonical-task-activity-migration.md) | Canonical Work-scope Task + the destructive CRM-Activity migration (operator ruling M5) | One canonical `Work` scope (Project + `archivable` Task + self-referential Subtask tree) absorbs CRM Activity verbatim (`kind`/`title`/`body`/`status`+`:in_progress`/`due_at`/`completed_at`/`custom`), adds `priority` (Samen.Type.Priority)/`owner_id`/`parent_id`/`project_id`, and links to CRM entities through the **generic `samen:<key>:<uuid>` object-ref** (`subject_key`+`subject_id`) — never CRM FKs — so **T43 builds it with ZERO CRM contact**. Status is a **plain enum, NOT ash_state_machine** (§4.3: the bulk migration inserts terminal-state rows directly; ADR-037 §5.8 scopes adoption to transition-guarding resources). **T97** destructively migrates every host's Activity (`act`/`fac`/`vce`/`swa`) into Task (id/org_id/timestamps verbatim, multi-anchor FKs preserved in `custom.crm_refs` — **zero data dropped**), rewires the CRM timeline/CDC/catalog reads, and drops the Activity table with **all four host mirror migrations in the same task** (no-deferral; clean drop/create gate; PITR is the contract-phase control). Binding **file-touch partition** (§7, reviewer-enforced via `git diff --name-only`); abbrev registry untouched by T97 (orphaned rows inert, ADR-023); `Core.Ctx.Activity` is a separate kernel fixture and a partition tripwire. |

## Client runtime posture (WS-UX ruling R1a)

| # | Title | Decision |
|---|---|---|
| [ADR-042](ADR-042-liveview-client-adoption.md) | LiveView client adoption + the progressive-enhancement contract (operator ruling R1a) | Reverses the no-JS/CSS-only interactivity posture (amends ADR-030's framing + ws-e E6-P2; trigger: dogfood B1 — every `phx-click`/`phx-submit` write browser-dead): ship the LiveView JS client platform-wide via the shared `Samen.Web.Layouts.root/1` with **no node toolchain** — prebuilt `phoenix.min.js`/`phoenix_live_view.min.js` served from the deps' own priv + a hand-authored ~30-line static `app.js` (client/server version skew structurally impossible; esbuild deferred behind a stated npm-dependency trigger); binding two-class progressive-enhancement contract (Class A auth/first-run/recovery spine keeps the T110 controller-POST no-JS floor forever, enhanced-in-place; Class B authenticated surfaces keep JS-off reads, writes may require the socket); masking stays server-resolved through `PiiResolution` (INV-1 by construction; watch-list unmodified); the CSS design system/kit is untouched; T113 implements contracts C1–C12 and lands only after T117 gates the generated operator routes. |

## The AI plane (WS-D, Phase 5)

| # | Title | Decision |
|---|---|---|
| [ADR-043](ADR-043-ai-plane.md) | The AI plane: no-PII-egress chokepoint, keyless kernel, provider packages, embeddings, MCP, eval posture (WS-D D1–D9) | Defines **INV-7 (no-PII-egress)**: no vault-routed value — plaintext or `vt_*` token — reaches any AI egress across **six enumerated classes** (completion prompts; tool args/result re-entry; embedding inputs→vectors; MCP responses; committed fixtures; **EG6 logs/telemetry/errors/exceptions**), with exactly ONE permitted exception: grant-covered plaintext in **ephemeral completion payloads only**, which **reaches the third-party model provider** and is therefore gated by an explicit host opt-in (`grant_plaintext_egress`, **default off**) on top of the live grant — a named, audited, accepted consequence, never a silent default. Testable form: *no string reaches a provider client except through `Samen.AI.Chokepoint`*. Enforced by construction (runtime-clause refusal + structural probe — deliberately NOT claimed as compile-time typing): provider clauses accept only a chokepoint-minted `%Samen.AI.MaskedPayload{}` (Inspect-redacting); the chokepoint resolves every binding through `PiiResolution` in a new **egress mode** (masked-but-present on EVERY plane, per INV-1's "AI call" clause), **re-scrubs conversation history per-turn** (grant-tagged spans re-mask when the grant lapses — no multi-turn accumulation), and **refuses fail-closed** (`{:error, :pii_egress_refused}`, payload-free, never strip); errors/logs/telemetry carry masked/token-only representations. Proven by `mix samen.verify.ai_prompt_masking` (T65) + a permanent vault-seeded-canary red-team CI tier asserting on recorded payloads, vector rows, AND captured logs/telemetry (T72). **Keyless by default** (M9): hand-built kernel per ash_ai REJECT (ADR-037 §5.6), two-callback `Samen.AI.Provider` behaviour, `samen_anthropic` as a separate fail-honest package (`{:error, :not_configured}` unwired — never a canned ok), deterministic payload-recording `Provider.Fake` + hashing `Embedder.Deterministic` as the CI lane, `SAMEN_AI_LIVE=1` the only live path. pgvector **REQUIRED** (M3): protocol-class dep, embeddings over catalog-allowlisted non-PII fields only (grants categorically never unlock embedding — vectors outlive grants and defeat crypto-shred), org-scoped vectors behind OrgScope. MCP (c4): HTTP+SSE in samen_web, per-operator tokens, masked/org-scoped tools, action *proposals* open T34 approvals and never execute. AI support operator drafts as a non-decider AI principal; send only via human approve (requester≠approver) through the Delivery chokepoint. Keyless grounding-eval bar fixed at **≥90%** — honestly framed as **context-assembly**, not model fidelity (that's the live lane); T64–T72 bound §-by-§. |

## The portfolio cockpit (WS-J, Phase 6)

| # | Title | Decision |
|---|---|---|
| [ADR-044](ADR-044-fleet-cockpit.md) | The portfolio cockpit: dual-mode fleet registry, token-blind report wire, cross-product operator identity, fleet directives, honesty from one product to N (WS-J J1–J5) | Operator ruling M10 — **BOTH** manual app registration as the baseline **and** opt-in self-registration/heartbeat for samen-generated apps behind a secured enrollment credential (ADR-035 §4.2's emitted-secret discipline reused verbatim). The cockpit is a first-party `samen_web` operator-plane LiveView (ADR-037 §5.13 ash_admin REJECT), never a generic admin renderer; the cross-product report wire is token-blind and proven by structural probe + a permanent CI tier (the ADR-043 shape); cross-product operator identity **extends** T146's `Samen.Web.Operator.Authz` rather than forking a second identity model, with tenant names masked by default and unmasked per-viewer via a dedicated per-product account-assignment model (absent row ⇒ `:none`, ruling R-A) that also gates tier-3 drill-in entry (ruling R-B); fleet directives are a new **layer** on the ADR-020 flag engine, not a second engine; cockpit-unreachable apps degrade **honestly** (observability without fleet flags, never a fake "applied", ruling 2). Amended 2026-08-06 — §16. |

## Pre-merge review dispositions (PR #1)

| # | Title | Decision |
|---|---|---|
| [ADR-045](ADR-045-premerge-review-dispositions.md) | LUMINARY pre-merge review dispositions: the tenant-gate prod default (V-F1) + the phased burn-down of what remains | **Accepted (2026-08-12).** After the two confirmed BLOCKERs were fixed and independently verified (B-SEC tenant-authn `on_mount`, `3b251e3`; B-OBAN queue-drain parity gate, `359abe7`), the merge-gating decision — the tenant gate is conditioned on `:auth_required?`, which **defaulted `false`** with no `config_env()` guard, so every shipped host + generated app shipped **disarmed** — was resolved by the operator choosing **Option A (fail-secure by environment, full-harden)** and shipping it: `9f14a61` (G2/V-F1, `:prod` armed by default + boot-refusal + generator `identity_namespace`) and `dc7b80d` (G1, membership-role derivation, closing S1a/S12), with the ADR-031 literal-default amendment now **in force** (§2.5). The two advised non-V-F1 items also CLOSED: O2 (pawchart `aud_chain`, `98125e5` G3) and X1 (generated-nav, `ead7a42` G4). Sabotage harness → **203**. The rest is filed as a four-phase backlog (1 gate integrity · 2 deploy/runtime hardening · 3 erasure completeness · 4 residual authz hardening), all honestly post-merge. Honesty posture (fail-honest, T144, no-overclaim) re-verified sound; T130 still blocked-safe and sequenced with D4. |

## Erasure completeness (Phase 3)

| # | Title | Decision |
|---|---|---|
| [ADR-046](ADR-046-erasure-completeness.md) | Erasure completeness: making `Samen.Erasure`'s carve-out list complete, and checkable-by-construction | **Accepted (2026-08-13); build COMPLETE (E1–E7, sabotage harness 212 → 227).** Designs — and now ships — the ADR-045 §4.3 Phase-3 cluster across seven independently-verified BATON batches (`71ba98b` E1+E2 · `e8aa2ea` E3 · `2f20b2d` E4 · `535620a` E5 · `740a8de` E6 · `97dabf3` E7). `Samen.Erasure`'s moduledoc names **two** carve-outs key-shred does not reach; there are **at least five** — each a plaintext-or-linkable value living *outside* the per-subject-DEK envelope. **D3 reachability answered: LATENT** (no shipped resource declares a `pii_declared: true` bag field; it is a runtime tenant capability) → framework-hardening, not a live leak. **D1** (recomputable `email_bidx` under the shared un-shreddable `sys:bidx`): recommend **tombstone to a random sentinel on principal-account erasure** — options (rotate `k_bidx` / re-key per-subject-DEK) rejected because they break pre-auth lookup + global dedupe; a genuine operator decision (ADR-035 §4.1 amendment). **D4/T130** (no blob deletion; clone aliases `storage_key`): build a fail-honest chokepoint `Storage.delete` **and** clone ref-counting in **one batch** — delete makes T130 live, so they ship together, never separately. **D5** (retention drops `org_id`), **D6** (DSAR no org binding), **D2/D7** (one-liners): mechanical. The durable fix is a **completeness verifier** (`mix samen.verify.erasure_completeness`, shipped E7) that discovers every out-of-envelope residue (blind indexes, `pii_declared` bags, `storage_key` blobs) and asserts erasure reaches each, with a non-empty-discovery floor — the check the grandfathering `pii_classify` baseline structurally cannot do. Operator decisions #1–#4 were TAKEN as recommended (D1 tombstone-to-sentinel; D4/T130 build-delete + last-reference ref-count); **one NEW operator decision #5 is OPEN (needs-operator-input)** — the E7 gate NAMES a residual (CRM Attachment `person_id`: a blob *about* a subject vs *uploaded by* one) with no destroy-caller today (nothing regressed), pending a retention-vs-erasure ruling. |

## The AI agent loop (EG2, post-Jido-eval)

| # | Title | Decision |
|---|---|---|
| [ADR-047](ADR-047-ai-agent-loop.md) | The AI agent loop: first-party durable multi-step tool use, governed by the EG2 egress class | **PROPOSED (2026-08-14) — design only, build NOT STARTED; §9's seven operator decisions are all OPEN.** Closes the gap the Jido evaluation recorded: ADR-043 §3.1 declares **EG2** (tool defs, tool args, tool-result re-entry) a governed egress class and T65 shipped `:history` per-turn re-scrub *for multi-turn loops*, but nothing implements it — no `tools:`/`tool_use` anywhere in `lib/`, no `lib/` caller of `history:`. Build `Samen.AI.Agent` **first-party, zero new deps**, inside the AST anti-bypass probe's and the sabotage harness's coverage (the decisive property against adopting Jido). Architecture: **checkpoint-per-turn, batch-per-job** — a durable `Agent.Run` cursor with a **never-nil `next_turn_at` watchdog** and `{run_id, turn_index}` turn-row idempotency (the `Samen.Sequences` shape, not Oban uniqueness), launched via the same-transaction `EventCapture` enqueue idiom; Reactor still runs the tool but cannot express dynamic step selection. Tools are a **four-way narrowing intersection** — `Automation.Action` registry ∩ explicit per-action opt-in (`tool_schema/0`, default `:not_a_tool`) ∩ the agent's declared list ∩ the actor's policy envelope — so no shipped action becomes a tool by accident; two **read-effect** actions join the same registry rather than forking a second allowlist; `webhook` and analytics are structurally ineligible. EG2 scrubbing is spelled out hop by hop: tool **defs** ride a new `:tools` field on `%MaskedPayload{}` scrubbed by `safe_metadata?/1` and must be **compile-time static** (a schema enum built from tenant data would be a silent per-turn egress); tool **results and arg echoes** re-enter ONLY as `PiiResolution`-egress-resolved binaries through `:history`, with `safe_segment?/1` as the fail-closed last line (the shipped sabotages 44/45 are re-proven on the agent path). **Grant plaintext is categorically excluded** from agent runs — the transcript persists and §7.2 forbids grants in persisted egress — a clarification of §6.1's "ephemeral" clause, not an amendment. **ADR-043 §6.2 holds unamended**: `effect: :write` tools do not execute, they open an E3 approval through `Samen.Approvals.Gate` (requester = the AI principal, decider = a distinct human, re-invoked as the requester with `authorize?: true`), parking the run in `:awaiting_approval`. Budgets (turns/tool-calls/tokens/wall-clock) are **fail-honest** — exhaustion is terminal `:budget_exhausted`, the last assistant turn is never promoted to a result; `cancel/2` is the codebase's first genuine interrupt, honestly framed as turn-boundary, not mid-call. Transcripts are **vault-routed** (inside the DEK envelope, run row as shred unit) + a default 90-day retention `:shred` spec, so ADR-046's discovery classes and named-residual list are unchanged. Proof: **9 new sabotages**, two new `ai_prompt_masking` structural checks, a new `mix samen.verify.agent_coverage` with a non-vacuity floor, an **EG2 arm on the permanent red-team tier**, MaskingCase three-proofs on both new surfaces, and a `Samen.AgentCase` kit + `mix samen.gen.agent`. Seven BATON batches **A1–A7** (loop core → durability → read tools → write proposals → surfaces → the driftwood support-triage v1 slice at ≈0 vertical LOC → the gates last); **≈2,450 authored non-test LOC** — the ~250-LOC dynamic-step-selection core the Jido report sized, plus the durability/governance/proof/UX that is why it is built rather than bought. |

## Context compaction (the ADR-047 loop's long-horizon contract)

| # | Title | Decision |
|---|---|---|
| [ADR-048](ADR-048-context-compaction.md) | Context compaction inside the ADR-046 erasure envelope: a dual-view transcript, a re-scrubbed summary, three-level overflow recovery, and withdrawal propagation on shred | **PROPOSED (2026-08-26) — DRAFT ONLY, nothing ratified, no code authored; all six §10 decisions and all six §11 open questions are OPEN.** Designs the contract the loop needs before anyone builds a compactor. Today there is none — `context_cutoff` / `token_budget` / `context.window` / `overflow_recover` are **zero hits** in `samen_core/lib` + `samen_web/lib`, `Samen.AI.Agent`'s moduledoc says *"performs no transcript compaction in v1"*, budget exhaustion is terminal and never promotes a partial answer, ADR-047 §11 names no compaction residual, and ADR-046 has **no derived-artifact rule**. Five proposed rulings: **(R1)** split the transcript into an LLM-facing view compaction may rewrite and an append-only UI-facing view it may not — **both under the SAME per-run DEK**, so shred units do not multiply and ADR-046's discovery classes are **unchanged by R1 — because the envelope is literally the same one, not because it was re-audited** (§4 non-negotiable 1); the split buys *rewritability*, never privilege (agent runs stay masked-only per §9#2). **(R2)** a summary is **new model-written text about masked content**, so it is born untrusted: the summarize call is an ordinary governed `complete/4` (`grant_egress?: false`, allowlist unchanged), and its **output** runs the full ingress path — `Secrets.redact/1` → `Ingress.sanitize/1` → §4.3 render → §3.2a on every later turn — because otherwise compaction is a **laundering channel** that re-admits exactly what T182/T184 just closed; refusal means *the fold does not happen*, never a partial fold. **(R3)** three-level overflow recovery — deterministic pre-turn fold (selection deterministic, the fold entry persisted in the cursor-advance transaction and **reused on replay**, never re-summarized), one mid-turn fold-and-retry on a new `:context_overflow`, then a NEW terminal `:context_exhausted` distinct from `:budget_exhausted` — with §9#3's **non-configurable fail-honest floor restated verbatim and unweakened**: no partial answer is promoted at any level. **(R4)** **containment first** — every compaction artifact lives inside the run's own DEK envelope, and compaction output is **categorically ineligible for embedding or any cross-run memory store** (ADR-043 M3's *"vectors outlive grants and defeat crypto-shred"* generalized to every derived artifact), so run-shred withdrawal is free, the way the trace-sink pseudonym is free. **(R5)** propagation only for the residue containment cannot reach — a **pseudonym-keyed** (`HMAC(psk_S, subject_id)`, the pattern ADR-046 §1 says *works*) token-only provenance index outside the sealed body, a bounded only-still-matching withdrawal walk registered as an erasure arm with a new `erasure_completeness` discovery class, neutralize-never-delete marking, the `ui_view` never rewritten, and a fail-honest `:source_withdrawn` terminal (the `:transcript_unavailable` precedent one level down). `:after_compaction` — declared by T181 with **no call site** — finally gets its caller, and stays narrowing-only (no `{:edit, _}`). **Amends nothing**: ADR-046's arms and ADR-047 §4/§6/§9 are consumed unchanged; the ADR-046 §7#5 / ADR-047 §11 third-party-free-text boundary is **pointed at, not settled** (§11 O-2), and the one link this draft cannot verify from the ADRs alone — whether the erasure pre-flight can carry a pseudonym into the steps-2–5 transaction without reordering the fail-safe key shred — is carried openly as §11 O-3. Ten proof obligations and a four-batch sketch (C1–C4) are recorded design; **no backlog rows filed**. |

---

49 ADRs total. All are `Status: Accepted` except ADR-025 (`Proposed (deferred)`) and **ADR-048**
(`PROPOSED` — draft only, no code authored, six decisions + six open questions all OPEN,
awaiting operator ratification); **ADR-047** is `Accepted` (2026-08-17, once its §9 seven
operator decisions were ratified — all seven TAKEN as recommended) with build status
`BUILT — A1–A7 all shipped`; ADR-046 was
Accepted on 2026-08-13 and its build is COMPLETE (E1–E7), with §7 decisions #1–#4 taken and one
new decision #5 carried OPEN (needs-operator-input) — the E7-named about-a-subject blob residual;
ADR-045 was Accepted on 2026-08-12 once its §2 (V-F1) decision was made and shipped. If you add a new one, append a row here in the same pass
(`docs/guides/cookbook.md`'s claim-evidence discipline applies to this index too — keep it honest,
not aspirational).
