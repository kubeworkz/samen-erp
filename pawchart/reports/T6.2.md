# T6.2 — PawChart Second-Vertical Thin Slice (reuse measurement)

**Phase 6 · task T6.2** — the plan's reuse-measurement probe.
**Date:** 2026-07-07
**App:** `/Users/clank/Desktop/projects/samen/pawchart/`
**Status:** GREEN. Full 17-step PawChart CI gate passes; root `ci.sh` green with the new pawchart arm; all prior suites (samen_core / demo / driftwood) unchanged and green.

---

## What was built

PawChart — a vet-clinic SaaS on the Samen substrate, the vision doc's **easy additive case** (contrast with Driftwood's bounded-context reshape).

- **(a) Two PII subjects, one relationship.** `PawChart.Clinic.Patient` = the human OWNER, composes `Samen.Fragments.CorePerson` (name/emails/phones vault-routed). `PawChart.Clinic.Pet` = the animal record, scalar vault field `pii_pet_microchip` (the doc's `pii_pat_microchip` idiom) + `belongs_to :owner` FK guarded by `Samen.Policy.SameOrgFk`.
- **(b) Plain Billing reused AS-IS.** `use Samen.Scopes.Billing` mounts 8 resources with NO reshape, NO `Samen.Context`, NO `alias_resource` — the deliberate contrast with Driftwood's `reshape Settlement` netting math.
- **(c) Tier-2 VaccineLot.** A custom object clinics define themselves via the inherited `Samen.CustomObjects` machinery (T3.9), org-scoped + contained + validated-at-write.
- **(d) Reuse measured.** `docs/reuse-measurement.md` — hard numbers, six-idiom mapping, time-to-gate, and an honest verdict.
- **(e) Full verifier gate green** on `pawchart/` (15 samen_core verifiers, all OK on first run).

## Red paths (must-fail tests) + anti-tautology probe

| Red path | File | Result |
|---|---|---|
| `pii_pet_microchip` vault round-trip (raw = `vt_` token, read = `%Masked{}`, shred → `shredded`) | `test/microchip_vault_test.exs` | 4 tests pass |
| Owner PII masked on the operator plane (operator key → ABSENT `%ForbiddenField{}`; impersonation → `%Masked{}` `••••`; tenant control reads CLEAR) | `test/operator_plane_masking_test.exs` | 3 tests pass |
| Tier-2 VaccineLot org-scoped + contained (org-scope, cross-org `[]`, one-way boundary, PII-shape rejection, validated-at-write) | `test/vaccine_lot_tier2_test.exs` | 5 tests pass |
| Cross-org denied (Patient/Pet OrgScope + same-org-FK refuses cross-org owner link) | `test/cross_org_test.exs` | 3 tests pass |
| **Anti-tautology probe** on the microchip vault path (baseline holds → SQL sabotage writes plaintext → assertions FLIP → revert restores token → green) | `priv/anti_tautology_probe.exs` + `test/microchip_anti_tautology_probe.md` | flip confirmed, `System.halt(1)` if not |

Plus `billing_reuse_test.exs` (3) and `dogfood_walkthrough_test.exs` (1) — **19 tests total, all green** with `--warnings-as-errors`.

## Reuse headline (see `docs/reuse-measurement.md` for the full accounting)

- **191 lines** of authored domain logic (`clinic.ex` 104 + `aggregate.ex` 42 + billing mount 17 + boilerplate 28) sit on **33,640 lines** of substrate.
- **4 of 6 idioms** applied with **zero vertical code** (self-qualifying storage, machine catalog, Postgres-as-engine, malleability ladder). PII vault cost **1 line**; the operator plane cost **42 lines** (one projection).
- Against the four substrate families a clinic exercises (~5,212 lines: Billing + CorePerson/base + Tier-2 + vault/mask/shred), authored domain logic is **~3.5%** → **~96% inherited**.
- **Time-to-gate:** all 15 verifiers green on **first invocation** — zero verifier fixes. The only two build failures were the vertical mis-using an inherited contract (a stray phoenix config line; string-vs-atom on the Billing `status`/`feature` fields).

## Honest verdict

The thesis validates **on the axis it claims** (doc §core: "you inherit INFRASTRUCTURE, not a domain model"): the infrastructure 80% is near-totally inherited; the domain 20% (the two nouns) stays authored real work. The additive case is **measurably** easier than Driftwood — it saved the entire `Samen.Context` + `reshape` layer (~90 lines of netting math + the context module). Honest asterisks recorded: the migration DDL (282 lines) is mostly generatable boilerplate (T6.4); the abbrev registry is global-not-per-host (T6.1 finding, re-confirmed — PawChart took fresh abbrevs `own/pet/pbc/…`); and the inherited feature vocabulary is a fixed bounded set.

## Substrate changes

- **`samen_core/priv/abbrev_registry.json`** — append-only: 11 reserved abbrev rows for PawChart (`own/pet/pag/pbc/pbs/pbl/ppc/pbi/pby/pbu/pbe`). No samen_core CODE changed.
- **`ci.sh`** (root) — added the pawchart arm after the Driftwood arm.

## Files

- `pawchart/lib/pawchart/{repo,application,billing,clinic,aggregate}.ex`
- `pawchart/priv/repo/migrations/` (9 shared infra migrations copied from the substrate bootstrap + `20260709100000_pawchart_resources.exs` authored)
- `pawchart/priv/{ci_bootstrap,anti_tautology_probe}.exs`
- `pawchart/test/*.exs` (6 test files) + `test/microchip_anti_tautology_probe.md`
- `pawchart/{ci.sh,mix.exs,schema.dict.json}`, `pawchart/config/*.exs`
- `pawchart/docs/reuse-measurement.md`
