# SABOTAGE: A3 (luminary pre-merge) — the generated-app gate parity is broken again: the
# B5 no_pan_columns step (ADR-038 §3.5; T23) is dropped from BOTH ci.sh generator
# templates (ci_sh.eex + ci_sh_api.eex), so every `mix samen.gen.app` output runs a
# strictly WEAKER verifier gate than the reference verticals — a raw-DDL card_number
# column that bypassed Ash is caught in demo/driftwood/pawchart (their step 15b/14b) and
# MISSED in an adopter's generated app, while the "correct-by-construction, passes its own
# gate" claim still prints green. Flips the A3 gate-parity guard, which asserts on the
# RENDERED template output for every configuration (so a golden re-capture cannot mask
# the regression); the byte-golden test fails alongside as drift evidence.
# APP: samen_core
# TEST_FILES: test/templates_parity_test.exs
# MUST_FAIL: every generated ci.sh runs samen.verify.no_pan_columns (A3 gate parity with the reference verticals)
diff --git a/samen_core/priv/templates/ci_sh.eex b/samen_core/priv/templates/ci_sh.eex
index 4f76d17..2315e0f 100644
--- a/samen_core/priv/templates/ci_sh.eex
+++ b/samen_core/priv/templates/ci_sh.eex
@@ -110,15 +110,6 @@ echo "--- step 14/18: mix samen.verify.no_pii_columns"
 mix samen.verify.no_pii_columns
 echo "    PASSED"
 
-# 14b. B5 no_pan_columns (ADR-038 §3.5; T23) — no resource/table ANYWHERE (every
-#      plane, not just aggregate) may carry a PAN/CVC-shaped column. Includes the
-#      information_schema sweep against the LIVE database, so a raw-DDL card_number
-#      column that bypassed Ash is caught here — the same enforcement the reference
-#      verticals (demo/driftwood/pawchart) hold themselves to.
-echo "--- step 14b/18: mix samen.verify.no_pan_columns (B5 no-PAN invariant)"
-mix samen.verify.no_pan_columns
-echo "    PASSED"
-
 echo "--- step 15/18: mix samen.verify.aggregate_privacy"
 mix samen.verify.aggregate_privacy
 echo "    PASSED"
diff --git a/samen_core/priv/templates/ci_sh_api.eex b/samen_core/priv/templates/ci_sh_api.eex
index 3734619..303af08 100644
--- a/samen_core/priv/templates/ci_sh_api.eex
+++ b/samen_core/priv/templates/ci_sh_api.eex
@@ -110,15 +110,6 @@ echo "--- step 14/19: mix samen.verify.no_pii_columns"
 mix samen.verify.no_pii_columns
 echo "    PASSED"
 
-# 14b. B5 no_pan_columns (ADR-038 §3.5; T23) — no resource/table ANYWHERE (every
-#      plane, not just aggregate) may carry a PAN/CVC-shaped column. Includes the
-#      information_schema sweep against the LIVE database, so a raw-DDL card_number
-#      column that bypassed Ash is caught here — the same enforcement the reference
-#      verticals (demo/driftwood/pawchart) hold themselves to.
-echo "--- step 14b/19: mix samen.verify.no_pan_columns (B5 no-PAN invariant)"
-mix samen.verify.no_pan_columns
-echo "    PASSED"
-
 echo "--- step 15/19: mix samen.verify.aggregate_privacy"
 mix samen.verify.aggregate_privacy
 echo "    PASSED"
