# T5.1 — Driftwood Domain Design (report)

**Task:** T5.1 (plan §7 PHASE 5) — DESIGN the Driftwood domain: context map,
settlement billing reshape, FMCSA gate, PII inventory, malleability-rung placement.
Spec-only; no app code (that is T5.2).

**Deliverable:** `/Users/clank/Desktop/projects/samen/driftwood/docs/driftwood-design.md` (756 lines).

## Repo-convention decision (noted per task)
Driftwood lives at **`/Users/clank/Desktop/projects/samen/driftwood/`** (a top-level
sibling of `samen_core/`, `demo/`, `spikes/`). The repo is NOT an `apps/` umbrella
(plan OD-1: single repo, siblings). Created `driftwood/docs/` and `driftwood/reports/`.

## Green-before / green-after (design-only, so baselines unchanged)
- **samen_core (before):** `768 passed (9 properties, 759 tests)` — green. The
  `[label-lint] FAIL:` lines are intentional red-path test output, not a suite failure.
- **demo (before):** `399 passed (17 properties, 382 tests), 48 excluded` — green.
- **After:** only `driftwood/docs/driftwood-design.md` + empty dirs were created; no
  file under `samen_core/lib` or `demo/lib` was modified (confirmed via `find -newermt`).
  Baselines therefore remain green. Root `ci.sh` unaffected.

## Substrate assertions verified against real code (not assumed)
- `Samen.Context` provides exactly `alias_resource` (name-only) + `reshape … calculate
  name, type, expr(...)` (NO storage; `:money`→`:decimal`); `NoStorage` transformer
  fails closed. (`samen_core/lib/samen/context/*`)
- Ash provides `Ash.Query.Function.If` (`if.ex`), comparison operators
  (`greater_than`/`less_than`), and division (`operator/basic.ex`) — so the settlement
  reshape's `expr(if(...))` clamp and `* bps / 10000` factoring calc are expressible.
- CRM/Billing blueprints expand into host-owned resources and accept an `abbrevs:`
  override; kernel Invoice money is integer cents (`amount_due_cents`). CorePerson
  composition, scalar `pii_` fields, reveal/vault, `SameOrgFk`, `OrgScope`,
  `RoleAtLeast` all confirmed. (`samen_core/lib/samen/scopes/*`, `fragments/core_person.ex`,
  `policy/*`; `demo/lib/demo/crm.ex`)
- The full 17-step verifier gate Driftwood's own `ci.sh` must replicate = `demo/ci.sh`.

## Settlement math verified numerically (decimal reference)
Ex1 (no factoring): $2000−$500 = **$1500**. Ex2 (3% factoring on $2150 gross): fee
$64.50, net **$2085.50**. Ex3 (advance>linehaul): net_raw −$324 → net_payable **$0**,
carryover **$324**. Ex4 (full stack): net **$1382.15**. Two-sided margin: $2500−$2000
= **$500**. All in the doc §3.5.

## Judge-panel result
Three settlement approaches scored (doc §3.6): A (reshape kernel Invoice via jsonb) 4/10
rejected; B (fully vertical, no Context) 6/10; **C (typed vertical `Settlement` inputs +
`Samen.Context` reshape for netting) 9/10 — CHOSEN**, because the reshape-in-the-bounded-
context is the exact claim T5 exists to prove.

## Decisions and open risks
Full lists in doc §8. Headline decisions: C2 one-Company + role discriminator + two
aliases; Activity→CheckCall alias + vertical DispatchEvent for the gated action;
Settlement vertical resource + Context reshape; negative-clamp-to-carryover; FMCSA gate
as a `before_action` change reading only non-PII dates + CDL token presence. Nine open
risks (OR-1..OR-9) hand off to T5.2, incl. the Company-role mechanism fork, the reviewed
`non_pii!` for `cdl_state/cdl_expiry`, integer-division truncation pinning, carryover
chaining, and the local game-day simulation seams (operator TODOs).
