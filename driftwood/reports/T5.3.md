# T5.3 — Both planes LIVE + dogfoodable LOCALLY (report)

**Task:** T5.3 (plan §7 PHASE 5) — make BOTH the tenant plane and the operator plane of
the Driftwood reference vertical live and dogfoodable on **localhost + local Postgres**
(the vision doc's Fly+Neon+AWS target is an OPERATOR TODO, documented as the deploy
seam). Tenant LiveView (broker dashboard, load board, driver roster w/ FMCSA status,
settlement view), operator plane over Driftwood tenants (masked impersonation w/ real
load board/driver roster + `••••` PII, second-party reveal end-to-end, token-blind
aggregate view), a scripted dogfood walkthrough + runbook, and a booted server with
evidence.

**Status: GREEN.** Full 18-step `driftwood/ci.sh` PASSES. Root `bash ci.sh` ALL PASSED
(spikes + samen_core 768 + demo 399 + demo CI + driftwood CI). `mix test
--warnings-as-errors` green in all three apps. The live server boots (Bandit @
127.0.0.1:4010) and serves every key page; all four LIVE red paths hold; the
anti-tautology probe on the masked-render path flipped and was reverted.

## Starting state (a prior session left the tree RED)

`lib/driftwood/aggregate.ex` + `aggregate/rebuild.ex` existed (created after the T5.2
report) but the app did NOT compile: the `dag`/`dtq` abbrevs weren't in the registry,
`Driftwood.Aggregate` wasn't in `ash_domains`, no aggregate migration existed, and there
was no web layer at all. T5.3 finished the aggregate wiring AND built the whole web +
dogfood + operator-plane surface.

## What was built (all absolute paths under /Users/clank/Desktop/projects/samen/driftwood)

**Aggregate plane wiring (fix + finish):**
- `samen_core/priv/abbrev_registry.json` — appended `dag`/`dtq` (data-only, append-only;
  samen_core 768 stays green).
- `config/config.exs` — added `Driftwood.Aggregate` to both `:driftwood` and
  `:samen_core` `ash_domains`; wired `impersonation_repo`, k/l floors (k=2/l=2, matching
  demo's small-dataset floor), query-budget ledger repo, and the `DriftwoodWeb.Endpoint`
  (Bandit adapter, local secret/salt).
- `priv/repo/migrations/20260707150000_aggregate_projections.exs` — `dag_load_volume_by_lane`
  + `dtq_mrr_by_tier` (catalogued in-tx; no `pii_` columns).
- `priv/repo/migrations/20260707180000_query_budget_ledger.exs` — `aqb_query_ledger`
  (T4.5 scaffold; catalogued).
- `lib/driftwood/operator_dashboard.ex` — cross-tenant `load_volume/0` + `mrr/0`, read
  ONLY through the token-blind aggregate domain with k-anon suppression handling.

**Tenant-plane rollup (rollup-backed dashboard, never raw scans):**
- `priv/repo/migrations/20260707160000_broker_summary.exs` — `dbs_broker_summary`
  (per-org load-by-status + settlement totals; catalogued).
- `lib/driftwood/broker_rollup.ex` — `run/2` recomputes the per-org summary; the
  settlement `net_payable` is reproduced in SQL exactly as the `Driftwood.Context`
  reshape (OR-5 truncated factoring fee), matching to the cent.

**Operator-plane kernel tables (so impersonation/reveal actually work):**
- `priv/repo/migrations/20260707170000_operator_plane.exs` — `imp_impersonation_session`
  (T4.1) + `osp_operator_suspension` / `brl_reveal_ledger` / `brc_break_glass_anchor`
  (T4.4). Token-only, uncatalogued (the demo precedent). Without these, `suspended?/2`
  fails closed (missing table = suspended) and `Impersonation.open` refuses.

**Shared read layer (the load-bearing masking seam):**
- `lib/driftwood/reads.ex` — `driver_roster/1`, `load_board/1`, `settlements/1`,
  `fmcsa_status/1`, `dispatchable?/1`. BOTH the broker's own console and the operator's
  impersonation view call the SAME functions differing only in scope: the impersonation
  scope has no reveal grant, so vault fields return `%Masked{}` and render `••••` BY
  CONSTRUCTION. Settlements read via `Samen.Context.reshaped_query/2` (calcs land in
  `row.calculations`).
- `lib/driftwood/operator_reveal.ex` — `reveal_cdl/2` drives the second-party reveal
  end-to-end via `Samen.Reveal.reveal/5` (single decrypt chokepoint).

**Web layer (served over HTTP by Bandit):**
- `lib/driftwood_web/{endpoint,router,page_controller,layouts,error_html}.ex`.
- `lib/driftwood_web/broker_live.ex` — TENANT plane (`/broker`): dashboard/loads/roster/
  settlements panels + the FMCSA-guarded Dispatch UI action (pure `dispatch_decision/2`).
- `lib/driftwood_web/operator_impersonation_live.ex` — OPERATOR masked impersonation
  (`/operator/impersonate`): real roster/load board with `••••`, accountability line,
  per-driver reveal control.
- `lib/driftwood_web/operator_dashboard_live.ex` — OPERATOR token-blind aggregate
  (`/operator/aggregate`): cross-tenant load volume + MRR, no PII, `⊘` for suppressed.
- `lib/driftwood/application.ex` — starts Repo + Oban + PubSub + Endpoint when the repo
  runs (dev/prod); web tree off in `:test`.

**Dogfood + runbook:**
- `lib/driftwood/dogfood_scenario.ex` — the single-source scenario builder (used by dev
  seed AND the test): 2 orgs, carriers/shippers, compliant + expired-card drivers, loads,
  dispatch, settlement, rollup + aggregate rebuild.
- `test/dogfood_walkthrough_test.exs` — the scripted end-to-end with assertions.
- `test/web_red_paths_test.exs` — the adversarial matrix vs the LIVE render path.
- `test/web_anti_tautology_probe.md` — the masked-render probe record.
- `docs/driftwood-dogfood.md` — the human runbook against the running server.
- `mix.exs` — added `bandit` / `phoenix_pubsub` / `jason`.
- `.gitignore` — ignore the dev KMS keystore (dev-only secret).

## Green-before / green-after

- **samen_core** before/after: **768 passed** (`--warnings-as-errors`) — the registry
  append is data-only, non-breaking.
- **demo** before/after: **399 passed** + demo CI gate ALL PASSED.
- **Driftwood** before: tree did NOT compile (aggregate abbrevs unregistered). After:
  full 18-step `ci.sh` ALL PASSED — 16 verifiers + **34 default tests** (1 property; was
  28 in T5.2, +6 from the dogfood + 5 red-path tests) + **4 adversarial**.
- **Root `bash ci.sh`: ALL PASSED.**

## Live-server evidence (T5.3 clause (d))

Boot log: `Running DriftwoodWeb.Endpoint with Bandit 1.12.0 at 127.0.0.1:4010 (http)`.
All key pages HTTP 200 (`/healthz`, `/`, `/broker?panel={dashboard,loads,roster,settlements}`,
`/operator/impersonate`, `/operator/aggregate`). Verified live via curl:

- **Broker dashboard** (rollup-backed): "2 open loads, $11000 gross"; settlement summary
  "1 settlements — net payable $4494.00" (read from `dbs_broker_summary`, never a raw scan).
- **Settlements** (reshaped money): $4800 linehaul − $500 advances − $156 factoring − $50
  claims = **$4494 net payable** (gross $5200 = linehaul+fuel+accessorial; factoring =
  trunc($5200×300bps) = $156). Two-sided money proven live.
- **Masked impersonation**: driver name AND CDL render `••••`; plaintext leak check empty;
  FMCSA badges visible (OK / BLOCKED: medical card expired); accountability line shows
  reason + expiry.
- **Aggregate**: lane `TX->CA` spans **2 tenants**, 4 loads, $22000 gross; MRR $5500 across
  2 tenants on `growth`; NO PII in the DOM.

## Red paths (HARD RULE 2) — the adversarial matrix re-run against the LIVE app

| Red path | Must-fail assertion | Non-vacuous control |
|---|---|---|
| Impersonation shows `••••` for CDL/name | `refute html =~ "CDL-OK-"` / name / `vt_` on the live render | the real data shape (2 driver rows + FMCSA badges) IS present |
| Ungranted reveal denies | `reveal_cdl` → `{:error, :denied}`; self-approval → `{:error, :self_approval}`; render stays `••••` | a distinct-party grant → `{:ok, "CDL-OK-…"}` |
| Aggregate exposes no PII | `refute html =~ "••••"/"CDL-"/name/driver_id` | it DOES render the lane cohort + MRR total |
| Expired-card driver can't dispatch from UI | `dispatch_decision` → `{:error, :fmcsa_blocked}`; button "Dispatch (blocked)" disabled | compliant driver → `{:ok, _}`, button enabled |
| Expired/absent session | render "access denied", 0 driver rows | (an open session renders the roster) |

Live-server confirmation reproduced all five (see clause (d) evidence above); the reveal
flow was also driven end-to-end against the live DB (before-grant denied, self-approval
refused, after-grant `{:ok, "CDL-OK-100"}`).

## Anti-tautology probe (scratch-dir sabotage → flip → revert)

**Masked-render path.** Backed the live view up to `_scratch_probe/`, then sabotaged
`OperatorImpersonationLive.load/3` to pre-fill `revealed` with each driver's PLAINTEXT
CDL (via `Samen.Vault.reveal/2`, bypassing the grant). Result: BOTH the red-path masked
test and the dogfood impersonation step FLIPPED to failing —
`refute html =~ "CDL-OK-"` failed with `CDL-OK-7298` / `CDL-OK-163` in the rendered DOM.
Reverted (restored the original, removed the scratch dir); the 6 tests pass again. Record:
`test/web_anti_tautology_probe.md`.

## Deploy seam (OPERATOR TODOs, documented not faked)

Runs LOCALLY: Phoenix/Bandit on `localhost:4010`, local Postgres (role `clank`, no
password), file-backed KMS (`priv/dev_keystore`). Real deploy seams (in
`docs/driftwood-dogfood.md`): Fly.io host + TLS front, Neon Postgres branch (PITR) via
`DATABASE_URL`, AWS KMS in place of `Samen.Kms.FileBacked`, env-injected `secret_key_base`
+ DB creds, and an authenticated session deriving the tenant org + operator identity
(the runbook uses query-param org/operator ids as a LOCAL dogfood convenience). Game-days
(T5.4/T5.5) remain local simulations reusing the T2.5 machinery.

## Honest residues

- The reveal control on the impersonation page is a `phx-click` (websocket); the live curl
  evidence proves the underlying `reveal_cdl` path end-to-end, and the dogfood test asserts
  the RENDER shows one driver's plaintext while others stay `••••`. A full browser-click
  reveal is exercised in-process by the test, not by curl (curl can't drive LV events).
- The broker's OWN roster also masks driver name/CDL (`••••`) — the tenant scope here
  carries no reveal grant either; the roster surfaces FMCSA STATUS, not driver PII (a
  deliberate posture, stated).
- `Driftwood.BrokerRollup` / `Driftwood.Aggregate.Rebuild` are plain functions the dogfood
  drives (not yet AshOban cron workers) — the same honest simplification the demo made;
  production wiring is a follow-up, not faked.
